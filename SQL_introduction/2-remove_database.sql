-- Script to drop the database hbtn_0c_0 if it exists
-- This script uses the DROP DATABASE IF EXISTS statement to avoid failure if the database doesn't exist

-- Drop the database if it exists
DROP DATABASE IF EXISTS hbtn_0c_0;
